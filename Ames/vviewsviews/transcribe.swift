//
//  transcribe.swift
//  Ames
//
//  Created by Ciana on 02/06/23.
//

//AMES-Achieve,Motivate,Excel,Succeed

/*
*import SwiftUI
import Foundation

struct transcribe: View  {
    var body: some View{
        if let folderURL = Bundle.main.url(forResource: "Scrumdinger", withExtension: nil) {
            if let fileURLs = try? FileManager.default.contentsOfDirectory(at: folderURL, includingPropertiesForKeys: nil, options: .skipsHiddenFiles) {
                return List(fileURLs, id: \.self) { fileURL in
                    Text(fileURL.lastPathComponent)
                }
            }
        }
        return (Text("Folder Content"))
    }
}

struct transcribe_Previews: PreviewProvider {
    static var previews: some View {
        transcribe()
    }
}*/
